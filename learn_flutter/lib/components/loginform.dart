import 'package:flutter/material.dart';

class _LoginFormState extends StatefulWidget {
  const _LoginFormState({super.key});

  @override
  State<_LoginFormState> createState() => __LoginFormStateState();
}

class __LoginFormStateState extends State<_LoginFormState> {
  final _formkey = GlobalKey<FormState>();

  @override
  Widget build(BuildContext context) {
    return Form(child: 
    // TODO Build form for user login and password and have some validation 
        );
  }
}
